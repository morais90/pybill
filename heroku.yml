setup:
 addons:
   - plan: heroku-postgresql

build:
  docker:
    web:
      dockerfile: Dockerfile
      target: production

release:
  image: web
  command:
    - release.sh

run:
  web: gunicorn olist.core.wsgi --bind=0.0.0.0:$PORT --workers=4
